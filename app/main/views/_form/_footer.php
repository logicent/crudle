<?php

echo $this->render('@appMain/views/_form_table/comments', ['model' => $model]);

// if ( $this->context->loadModal ) : 
//     $this->registerJs( $this->render('_modal_submit.js') );
// endif;
